#include "Collider.h"

Collider::Collider() : Component()
{

}

void Collider::calculateColliderBox()
{
	//cWidth *= scale.x;
	//cHeight *= scale.y;
}

void Collider::calculateColliderBoundaries()
{
				// From transform			// Offset
	/*cLeft = position.x + (texture.mWidth * texture.scale.x - mColliderBox.w) / 2;
	cRight = position.x + mColliderBox.w + (texture.mWidth * texture.scale.x - mColliderBox.w) / 2;
	cTop = position.y + (texture.mHeight * texture.scale.y - mColliderBox.h) / 2;
	cBottom = position.y + mColliderBox.h + (texture.mHeight * texture.scale.y - mColliderBox.h) / 2;*/
}